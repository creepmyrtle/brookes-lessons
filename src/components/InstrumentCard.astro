---
interface Props {
  icon: string;
  title: string;
  description: string;
}

const { icon, title, description } = Astro.props;
---

<div class="instrument-card">
  <div class="instrument-icon">{icon}</div>
  <h3>{title}</h3>
  <p>{description}</p>
</div>
